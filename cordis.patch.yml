# dsh-oneoffice roster patch — merged by `dsh plugin add` (dsh.bundle.patch).
# Mounts the host half so the client bundle reaches window.__DSH_BOOT__.
- insert:
    - id: dsh-oneoffice
      name: 'dsh-oneoffice'
